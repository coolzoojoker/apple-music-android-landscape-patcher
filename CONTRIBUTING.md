# 参与开发

欢迎提交可复现的问题和通用修复。修复应针对状态或生命周期根因，不使用临时覆盖层遮挡画面问题。

贡献代码前请确保：

1. 不提交 Apple Music APK、APKM、DEX、资源或大段反编译代码。
2. 不提交签名密钥、密码、账号、设备 IP 和未脱敏日志。
3. 补丁必须验证输入版本与唯一锚点；不匹配就失败，不能静默继续。
4. 用通用的 ABI、密度、生命周期和播放器状态规则解决问题，不针对一首歌、一个封面或一个坐标写特例。
5. UI / 动画修改只触及问题涉及的状态，并记录 HOME、歌词、待播、收起与再次打开的回归结果。

提交前运行：

```powershell
.\scripts\audit-release.ps1
```
