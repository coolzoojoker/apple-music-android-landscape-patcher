# 兼容性与构建配置

当前补丁只接受 **Apple Music 6.5.0 (1580)** 的指定 APKM，最低系统为 Android 11（API 30）。构建器会校验输入文件 SHA-256，不匹配时直接停止。

| 配置 | CPU / 密度 | 用途 | 验证状态 |
|---|---|---|---|
| `tv-armv7` | armeabi-v7a / 全密度 | 32 位 Android TV、部分电视 SoC | 索尼 K-85XR70 实机路线 |
| `tv-arm64` | arm64-v8a / 全密度 | 主流 64 位 Android TV / Google TV | 待更多机型反馈 |
| `car-arm64` | arm64-v8a / 全密度 | 可安装第三方 APK 的 ARM64 车机，附沉浸式系统栏处理 | 已在用户车机验证基础功能；其他车型试验性 |
| `tv-armv7-xhdpi` | armeabi-v7a / xhdpi | 索尼测试机的精简包 | 实机基准配置 |

“全密度”是为了覆盖不同厂商对 `densityDpi` 的设置，不代表针对每个分辨率都做过逐像素适配。电视实际输出 4K 时，应用窗口也可能由系统按 1920×1080 逻辑分辨率渲染再缩放，这是 Android TV 常见行为。

## 为什么不按 Android 11 / 12 / 13 / 14 分四份

原版的最低版本就是 Android 11，当前补丁使用的 API 在 11+ 共用。现阶段真正影响安装和画面的主要因素是 CPU 架构、密度和厂商系统栏，而不是 Android 大版本。只有日志证明某个系统版本需要不同实现时，才会新增独立配置，避免产生无法验证的“假兼容包”。

## 车机范围

车机包面向类似比亚迪 DiLink、允许安装第三方 APK 的 ARM64 Android 车机。厂商密码、安装入口、驾驶安全限制、系统签名要求可能因车型和固件而异；补丁不会绕过车机安全策略。驾驶时请勿操作播放器界面。

